export class Book {

    constructor({id, title, synopsis, characters, locations}) {
        this.id = id
        this.title = title
        this.synopsis = synopsis
        this.characters = characters
        this.locations = locations
    }
    


}
